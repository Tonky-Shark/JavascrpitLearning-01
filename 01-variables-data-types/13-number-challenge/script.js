let x = Math.floor(Math.random() * 100 + 1);
let y = Math.floor(Math.random() * 100 + 1);

let sum = x + y;
let difference = x - y;
let product = x * y;
let qutient = x / y;
let rm = x % y;

console.log("sum", x, "+", y, "=", sum,
    "\ndif", x, "+", y, "=", difference,
    "\nprod", x, "+", y, "=", product,
    "\nqutinet", x, "+", y, "=", qutient,
    "\nrm", x, "+", y, "=", rm);